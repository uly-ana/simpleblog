<h1>All Posts</h1>
<% @post.each do |post| %>
  <div class="alert alert-light">
  <h3><%= post.title %></h3>
  <p><%= post.body %></p>
  <%= link_to "Read more", post_path(post), :class => 'btn btn-link' %>
</div>
<% end %>
